{
  "name": "提交信息规范检查",
  "description": "校验Git提交信息是否符合Conventional Commits规范，满足变更可追溯要求",
  "triggers": ["PreCommit"],
  "hooks": [
    {
      "type": "command",
      "command": "npx commitlint --edit $1",
      "timeout": 30
    }
  ]
}