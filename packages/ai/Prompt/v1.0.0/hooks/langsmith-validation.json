{
  "name": "LangSmith配置验证",
  "description": "验证提示词是否正确配置了LangSmith用于测试和可观测性",
  "triggers": ["PreCommit"],
  "matcher": "*.prompt.ts",
  "hooks": [
    {
      "type": "command",
      "command": "npx tsx scripts/check-langsmith-config.ts",
      "timeout": 20
    }
  ]
}