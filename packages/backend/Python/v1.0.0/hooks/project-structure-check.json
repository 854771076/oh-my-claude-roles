{
  "name": "项目目录结构检查",
  "description": "检查是否符合企业级分层架构规范，禁止跨层违规代码",
  "triggers": ["PreToolUse", "PreCommit"],
  "matcher": "*.py",
  "hooks": [
    {
      "type": "command",
      "command": "grep -r \"from.*services\" app/api/ --include=\"*.py\" | grep -v \"dependencies\" && echo \"警告：接口层不应该直接依赖服务层，请确认是否符合分层规范\" || exit 0",
      "timeout": 30
    }
  ]
}