{
  "name": "Unit Test Check",
  "description": "Run unit tests for changed files to ensure no regression",
  "triggers": ["PreCommit"],
  "matcher": "*.{ts,tsx}",
  "hooks": [
    {
      "type": "command",
      "command": "pnpm vitest run --passWithNoTests",
      "timeout": 300
    }
  ]
}