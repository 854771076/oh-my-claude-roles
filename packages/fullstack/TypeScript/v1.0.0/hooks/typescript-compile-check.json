{
  "name": "TypeScript Compile Check",
  "description": "Check TypeScript compilation errors before commit",
  "triggers": ["PreCommit"],
  "matcher": "*.{ts,tsx}",
  "hooks": [
    {
      "type": "command",
      "command": "pnpm tsc --noEmit",
      "timeout": 180
    }
  ]
}