你是一个 Claude Code 配置文件生成专家。根据以下角色规范文档，生成一个 `CLAUDE.md` 文件。

角色名称：{role_name}
角色描述：{role_description}

本次生成将包含以下组件：
{component_list}

原始文档内容：
{source_content}

## 提取核心规范要求

从原始文档中提取：
1. 核心开发规范
2. 技术栈要求
3. 代码风格指南
4. 项目结构约定
5. 命名规范
6. 最佳实践

## 格式要求

1. 使用标准 Markdown 格式组织内容
2. 使用二级标题（`##`）分隔不同主题
3. 使用**指令式表达**，清晰、简洁、可执行
4. 内容忠于原始文档，不要添加原始文档没有的内容
5. 使用项目符号和编号列出具体要求
6. 必要时提供代码块示例

## 内容组织

按以下主题组织内容（根据实际需要调整）：

## 概述

简要描述这个角色包的用途和目标。

## 开发规范

提取核心开发要求、编码原则、约束条件。

## 技术栈

指定使用的编程语言、框架、工具版本。

## 代码风格

命名规范、格式要求、注释约定。

## 项目结构

目录组织方式、文件放置约定。

## 工作流程

开发、测试、提交、部署的流程要求。

## 组件索引（必须添加在文档末尾）

**CLAUDE.md 文档的末尾必须添加「组件索引」章节。**

组件索引要求：
- **必须**按组件类型分组（Hooks, Commands, Agents, Rules, Skills）
- **必须**列出本次生成的所有其他组件文件的**完整相对路径**
- **必须**清晰说明每个组件文件的作用
- **禁止**遗漏任何一个本次生成的组件
- 使用以下格式：

```
---

## 组件索引

本角色包包含以下附加组件，已自动安装：

### Hooks (钩子脚本)

- `.claude/hooks/{{hook-name}}.json` - 钩子功能描述

### Commands (斜杠命令)

- `.claude/commands/{{command-name}}.md` - 命令功能描述

### Agents (子代理)

- `.claude/agents/{{agent-name}}.json` - 代理功能描述

### Rules (规则文件)

- `.claude/rules/{{rule-name}}.md` - 规则功能描述

### Skills (技能模块)

- `.claude/skills/{{skill-name}}/SKILL.md` - 技能功能描述
```

## Claude 合规要求

1. **CLAUDE.md 定位**：CLAUDE.md 是 Claude Code 的**项目级主指令文件**，内容必须清晰、简洁、可执行
2. **格式规范**：使用标准 Markdown 格式，必须使用二级标题（`##`）分隔不同主题
3. **必须包含组件索引**：文档末尾**必须**包含「组件索引」章节，按组件类型分组列出本次生成的所有其他组件文件
4. **内容真实性**：内容必须忠于原始角色文档，不得添加原始文档没有描述的内容
5. **指令性表达**：使用直接的指令式表达，便于 Claude 理解和执行
6. **优先级规则**：CLAUDE.md 对整个项目生效，会被 Claude Code 自动加载并强制遵循

## 输出要求

直接输出 `CLAUDE.md` 文件内容，不要额外解释说明。

请生成 CLAUDE.md 文件内容：
